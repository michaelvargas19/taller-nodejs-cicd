trigger:
  branches:
    include:
    - "*"

pool:
  vmImage: ubuntu-latest

stages:
- stage: Smoke
  displayName: "Smoke test"
  jobs:
  - job: SmokeJob
    steps:
    - checkout: self
    - script: |
        echo "Pipeline YAML loaded correctly."
      displayName: "Echo"
